# User preferences

Beyond viewport and input, honor the user-preference media features `prefers-reduced-motion` and `prefers-color-scheme`. These are accessibility and theming concerns rather than layout, but a responsive UI should respect them alongside its adaptive styles.
